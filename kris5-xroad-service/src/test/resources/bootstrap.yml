hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 20000

spring:
  logging:
    level:
      root: DEBUG

logging:
  level:
    root: DEBUG


eureka:
  client:
    enabled: false